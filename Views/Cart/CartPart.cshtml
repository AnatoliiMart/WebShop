@model WebShop.Models.ViewModels.Cart.CartVM

<div class="ajaxcart">
    Your cart: @Model.Quantity item(s) - @Model.Price UAN
    <br />
    @Html.ActionLink("Go to cart", "Index", null, new {@class = "btn btn-success"})
</div>